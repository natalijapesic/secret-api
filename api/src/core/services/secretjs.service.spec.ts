import { SecretJsService } from 'core/services/secretjs.service';

describe('SecretJS Service', () => {
  it('should query all codes', async () => {
    const client = new SecretJsService();

  });
});
